# Host credentials are intentionally optional at scaffold time.
# Fill only the adapters you plan to run locally.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Logos runtime defaults.
LOGOS_CONFIG=.logos/config.yaml
LOGOS_DEFAULT_HOST=gemini
LOGOS_RUNS_DIR=runs
LOGOS_REPORTS_DIR=reports
