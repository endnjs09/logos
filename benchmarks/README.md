# Benchmarks

Benchmark assets define repeatable tasks for comparing:

- Codex baseline
- Codex + Logos

The benchmark layer should keep task inputs, fixtures, expected outcomes, and
suites separate from runtime implementation.
