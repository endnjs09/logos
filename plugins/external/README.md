# External Plugins

External or local-only Logos plugins can be placed here.

External plugins should not be trusted by default. Future plugin loading should
validate manifests, schemas, hooks, and tool permissions before activation.
